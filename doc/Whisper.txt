Models
	RemoteSwitch
		Label (becomes Device/name)
		Location (becomes Device/location)
		Type (becomes Device/model)
	Device
		name
    protocol
    model
    house
    unit
    location
	
When a device i created, a run-on-schedule-file is also created in [location]. Cron will be set up to run this file every 5 minutes.